import 'package:flutter/material.dart';

class ScoreResultView extends StatelessWidget {
  int scores = 0;
  ScoreResultView(int score) {scores = score;}
  @override
  Widget build(BuildContext context) {
    
    return null;
  }
}
